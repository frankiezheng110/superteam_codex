---
name: resume
description: Resume a paused SuperTeam Codex run.
disable-model-invocation: true
---

# SuperTeam Codex Resume

Run:

```powershell
python "<plugin-root>\superteam_codex\cli.py" --project "<project-root>" resume
```

Then continue from the stage in `mode.json`.

