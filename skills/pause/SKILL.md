---
name: pause
description: Pause an active SuperTeam Codex run without deleting artifacts.
disable-model-invocation: true
---

# SuperTeam Codex Pause

Run:

```powershell
python "<plugin-root>\superteam_codex\cli.py" --project "<project-root>" pause
```

Do not continue orchestration until the user resumes.

