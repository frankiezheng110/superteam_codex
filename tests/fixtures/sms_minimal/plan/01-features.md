# SMS Feature List

## S0 Settings

The settings page must use Pencil frame `d86C1`.

## S3 Compare

The comparison page says its Pencil frame is `e0Qlt`, but that frame is not in
the current `.pen` file. SuperTeam Codex must block execution instead of
inventing a replacement page.

